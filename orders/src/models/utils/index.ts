// export const remapId = <T extends any>(attrs: T) => {
//   return Object.entries(attrs).reduce((acc, [key, val]) => {
//     if (key === "id") {
//       acc["_id"] = val;
//       return acc;
//     }

//     acc[key] = val;
//     return acc;
//   }, {});
// };
